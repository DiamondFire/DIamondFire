sourceCompatibility = 1.7

run {
    ignoreExitValue = true
}

mainClassName = "utm.csc492.diamondfire.DesktopLauncher"

sourceSets.main.resources.srcDirs = [file("../android/assets").getAbsolutePath()]

eclipse.project {
    name = appName + "-desktop"
}